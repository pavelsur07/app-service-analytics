# Шаблон пакета для внешнего ревью

Собирается `make review-prepare` в `var/review/package.md`. Правки процесса —
в `CLAUDE.md` → «Процесс работы над задачей», не в этом файле.

---

## 1. Задача

<!-- одним абзацем: что менялось и зачем -->

## 2. Диф

```diff
<!-- git diff относительно базовой ветки -->
```

## 3. Обязательные правила

<!-- раздел «Обязательные правила» из CLAUDE.md целиком, без сокращений -->

## 4. Релевантные ADR

<!-- полные тексты ADR, затронутых изменением -->

## 5. Инструкция ревьюеру

Проверяй соответствие правилам и ADR из разделов 3–4 выше, а не предлагай
общепринятые альтернативы (API Platform, `findAll()`, Redux для серверного
состояния, Symfony Forms, глобальные фикстуры — здесь отвергнуты сознательно,
см. ADR).

Если не согласен с самим правилом — скажи об этом отдельным пунктом
«пробел в правилах», а не оформляй как дефект кода.

Ты не запускаешь тесты и не собираешь проект — только читаешь.
