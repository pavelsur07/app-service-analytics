# План I-1: заказы и исходы для Planning в Ingestion

**Результат:** `IngestionPlanningFacade` отдаёт проверяемые когорты заказов, исходы и даты наблюдений строго внутри компании и кабинета. Модуль Planning не читает таблицы Ingestion. Зависимость — [контракт 0.1](sales-planning-stage-0-1-implementation.md); бизнес-граница — [ADR-024](../adr/0024-sales-planning-and-inventory-boundaries.md). Этот план описывает отдельное изменение Ingestion, без формул и таблиц Planning.

## Сверка перед кодом

Прочитать `BuyoutOutcomeQuery`, текущую view `buyout_outcome` в последней миграции, `FetchOzonPostingsHandler`, `FetchOzonReturnsHandler`, `MarketplacePostingStatusRepository`, `SalesFactRepository`, `CompanySkusQuery`, ADR-006/019/020. Сейчас `BuyoutOutcomeQuery` возвращает один исход на строку и `resolved_at`, но не даёт закреплённой версии выборки, полноты прогона, количественных аллокаций и двух независимых осей времени. `FetchOzonPostingsHandler` делает один запрос на день и отклоняет ответ у лимита; до объявления дня полным убедиться, что этот потолок не достигнут. Отдельно проверить, как количественный возврат отражается в view, не размножая заказанную единицу.

## Карта реализации

| Место | Изменение |
| --- | --- |
| `api/src/Ingestion/Application/Facade/IngestionPlanningFacade.php` и readonly DTO рядом | Методы `planningOrderCohorts`, `planningResolutionTotals`, `planningResolutionObservations`, `knownMarketplaceSkus`, `searchMarketplaceSkus` из 0.1; только данные кабинета, без Entity и секретов |
| `api/src/Ingestion/Infrastructure/Query/PlanningOrderCohortsQuery.php`, `PlanningResolutionTotalsQuery.php`, `PlanningResolutionObservationsQuery.php`, `PlanningMarketplaceSkusQuery.php` | Ограниченные DBAL-запросы; агрегация количеств в PostgreSQL; keyset для списков |
| `api/src/Ingestion/Domain/` и `api/src/Ingestion/Infrastructure/Repository/` | Типы оси времени, сущности схемы и upsert записи доказанных аллокаций/маркеров полноты; конкретный набор определить по пробелам при сверке view |
| `api/src/Ingestion/Application/Message/FetchOzonPostingsMessage.php`, `FetchOzonReturnsMessage.php`, `Application/DispatchActiveOzonSyncsAction.php` | Явно различать регулярный короткий опрос и глубокий рескан/backfill в сообщении; прежние сообщения очереди разбирать совместимо |
| `api/src/Ingestion/Application/MessageHandler/FetchOzonPostingsHandler.php`, `FetchOzonReturnsHandler.php` | Фиксация происхождения и нового полного наблюдения после всех успешных записей, включая повтор идентичного raw; отказ не публикует полноту |
| `api/migrations/Version<UTC>.php` | Одна итоговая миграция только для реально необходимых маркеров/аллокаций, индексов и возможной новой view; старые миграции не править; `down()` либо явное восстановление |
| `api/deptrac.php`, `api/bin/check-src-structure.sh`, `Makefile` | Узкий слой Facade, если нужен по ADR-024; список модулей менять только при первом новом модуле, не ради I-1 |
| `api/tests/Unit/Ingestion/`, `api/tests/Integration/Ingestion/`, `api/tests/Functional/Ingestion/` | Тесты классификации, SQL-изоляции, полноты и контрактов Facade; данные через Builder |

## Контракт и последовательность

1. Выделить из заказов количественные аллокации с ключом `(company, account, sourceRowId, allocationKey)`. Для одной когорты сумма `D + R + T1 + T2 + P + unknown` равна `ordered`; `D` и `R` дают `bought`, `T1/T2/P` — `terminalNoBuy`, только доказанно открытое состояние — `openEligible`. Противоречивое свидетельство о возврате и недатируемый исход получают качество, а не придуманную дату. Не менять существующий отчёт выкупа ADR-020.
2. Добавить в сообщение происхождение опроса (`regular` для дат короткого окна, `rescan` для более старых дат), передав его из существующего диспетчера и команд дозагрузки; в день глубокого рескана даты короткого окна всё равно маркируются `regular`. Для уже поставленных сообщений предусмотреть безопасную совместимость формата. Сохранить `firstKnownOutcomeAt` как первое достоверное подтверждение исхода и отдельно `firstRegularlyObservedAt` для регулярной скорости. `sourceEventAt` заполнять только если событие доказано самим ответом Ozon. У дозагруженного старого исхода без такого доказательства обе первые даты остаются `NULL`, запись помечается `backfill` и не попадает в сегодняшний поток/окно коэффициента. `resolved_at` view сам по себе не доказывает время события.
3. Для каждого кабинета/периода фиксировать версию полного опроса и момент проверки отдельно от ID raw-документа: одинаковый raw при новом успешном опросе обновляет свежесть, ошибка разбора/лимита не делает период полным. Версию менять при исправлении любой аллокации или охвата; сформировать устойчивый непрозрачный токен для всех пяти методов Facade.
4. Реализовать SQL с `company_id` и `marketplace_account_id` **в базовых выборках**, а не после агрегации. `planningResolutionTotals` группирует по SKU и исходу в БД, принимает максимум 200 SKU и обе оси времени; неизвестные даты в окно не входят. Пустой массив SKU возвращает пустой результат. Страницы когорт/наблюдений/поиска имеют лимит 50 по умолчанию, максимум 200, `limit + 1` и keyset; курсор закрепляет компанию, кабинет, фильтр, ось и версию, чужой либо устаревший курсор отклоняется. Для соседних страниц расчёта Planning сверяет общую версию.
5. Поиск SKU строить по `marketplace_listing` и `sales_fact` **выбранного кабинета**; снятые с продажи SKU остаются доступными для истории. При поиске по артикулу и имени индекс/план запроса проверить отдельно; не использовать `COUNT(*)` по факт-таблице.

## Проверки и завершение

- Unit: все `T1/D/T2/P/R/NULL`, частичные количества, возврат после выкупа, backfill, неизвестная дата, сумма аллокаций.
- Integration: одинаковый SKU у двух компаний и двух кабинетов; неизвестный и поддельный курсор; обновление версии между страницами и пакетами; повтор raw, исправление исхода, обрыв полной загрузки; SQL `EXPLAIN` на объёме чужого кабинета.
- Проверить миграцию с пустой базы, `make lint stan deptrac structure-check`, затронутые `make test-unit test-int test-func`, `make audit`; точные команды и результаты занести в пакет задачи. Коду/схеме/изоляции нужен Claude и оба прохода Codex согласно `AGENTS.md`.

**Готовность:** у любого количества есть исход, время и источник; один выкуп не находится одновременно в факте и открытом прогнозе; данные соседнего кабинета не меняют результат; неполный опрос явно виден и не выдаётся за полный.
