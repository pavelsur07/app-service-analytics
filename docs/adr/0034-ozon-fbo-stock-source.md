## ADR-034: Источник остатков Ozon FBO по кластерам

**Дата:** 2026-09-26
**Статус:** Proposed
**Модуль:** Ingestion
**Уточняет:** ADR-006 (raw-слой и прослеживаемость — как есть; окно
пересчёта, глубокий рескан и построчная битемпоральность к снимкам не
применяются, см. «Решение»),
ADR-021 (остатки глубины не имеют)

### Контекст

Третий продуктовый блок — анализ остатков — в коде отсутствует. Варианты A
и B отчётов по доставке показали, что половина продаж нелокальна, и
нелокальная доставка идёт в 2,2 раза дольше, но не могут ответить, почему
товар везли издалека: его не было в кластере или он кончился. Для этого
нужен остаток по кластерам — сегодня у нас только сумма по SKU из
`/v3/product/info/list`.

Разведка (`docs/task/ozon-stocks-research.md`, живые ответы кабинета
2026-09-26) показала, что `/v1/analytics/stocks` отдаёт остаток на уровне
SKU × склад с кластером в каждой строке, виды остатка (доступно, в пути,
заявлено к поставке, возвраты, брак) и показатели Ozon по складу и кластеру.
Названия кластеров совпадают с `cluster_to` в продажах побайтово.

Решение необратимо в двух местах: зерно и ключ факт-таблицы, и то, что
история копится только вперёд — задним числом остатки не загрузить
(ADR-021), поэтому потерянный день снимка не восстанавливается.

### Решение

**Источник — `POST /v1/analytics/stocks`** с телом `{skus: [...]}` пачками
по 100 SKU; список SKU — из каталога, который коннектор уже загружает.
Ответ сохраняется в raw как у всех источников (ADR-006, ADR-024), тип
отчёта `ozon_analytics_stocks`, **период raw-документа — `snapshot_date`**:
даты в запросе нет, и без неё неизменный ответ следующего дня не дал бы
своего документа. С датой в периоде raw-пачки разных дней не смешиваются;
какие именно пачки дали текущий снимок дня (за день бывает несколько
прогонов, включая частичные и устаревшие), хранит отметка дня
(`raw_document_ids`) — по ней день восстановим из raw.

**Факт — `stock_snapshot_fact`, зерно — снимок дня × SKU × склад.**

```
PRIMARY KEY (company_id, marketplace_account_id, snapshot_date, source_row_id)
source_row_id = sku | warehouse_id
```

`snapshot_date` — дата **начала прогона** снимка (первый запрос пачки)
в часовом поясе площадки, **отдельной
колонкой первичного ключа**, а не частью склеенной строки, как у других
фактов (ADR-006, ADR-012). Отступление сознательное: таблица растёт
линейно времени (см. «Платим»), и единственный разумный выход —
партиционирование по `snapshot_date`; в PostgreSQL колонка партиционирования
обязана входить в первичный ключ. Добавить её потом — смена ключа на
накопленной истории, которую нельзя перезагрузить.

Колонки: `marketplace_sku`, `warehouse_id`, `warehouse_name`, `cluster_id`,
`cluster_name`, остатки по видам (целые): `available`, `transit`,
`requested`, `return_from_customer`, `return_to_seller`, `defect` (сумма двух
видов брака), `other` (прочие виды одной суммой — ради сверки с кабинетом),
показатели Ozon справочно: `ads_cluster`, `idc_cluster`,
`turnover_grade_cluster`; `raw_document_id` каждой строки (прослеживаемость).

**Снимок дня — атомарная единица, а не набор независимых строк.** Площадка
не отдаёт строку при нулевом остатке (разведка: карточки с нулевым FBO
в ответе отсутствуют), поэтому «ноль» и «снимка не было» выглядят одинаково
— отсутствием строки. Отсюда:

- **прогон** — все пачки SKU каталога одного подключения, запрошенные
  одной обработкой сообщения. `started_at` — момент первого запроса
  прогона; `snapshot_date` — его дата по Москве, и прогон пишет только
  свой день, даже если закончился после полуночи. **Побеждает прогон
  с более поздним `started_at`.** Блокировки прогонов нет: каждый прогон
  внутренне согласован и заменяет день целиком, смешивания не бывает, а
  два перекрывающихся прогона одного дня расходятся на минуты при
  суточном зерне. Повтор сообщения очередью — новый прогон с новым
  `started_at`: он заменяет день актуальным снимком;
- **отметка снимка** — таблица `stock_snapshot_run`, первичный ключ
  `(company_id, marketplace_account_id, snapshot_date)`; колонки:
  `started_at` — начало прогона, давшего текущий снимок дня (NULL —
  полного ещё нет); `first_started_at` — начало первого полного прогона
  дня, после заполнения не меняется; `requested_skus` — список
  запрошенных SKU (массив); `raw_document_ids` — raw-документы пачек
  прогона, давшего текущий снимок (массив); число строк. **Снимок полон**,
  когда успешно получены все пачки прогона; **частичный прогон ничего
  не пишет** — ни в факт, ни в отметку, и полный снимок дня, записанный
  раньше, остаётся как был;
- **ноль** — пара SKU × кластер, отсутствующая в полном снимке дня, при SKU
  из `requested_skus` этого дня. SKU, которого в запросе не было (товар
  появился или ушёл из каталога), — «неизвестно», а не ноль. **День без
  полного снимка** — неизвестный: из расчёта спроса он исключается как
  неизвестный, а не как дефицитный;
- **запись полного прогона** — одна транзакция: отметка дня вставляется
  `INSERT … ON CONFLICT DO NOTHING` с `started_at = NULL` (уникальный ключ,
  без «найти, и если нет — записать», CLAUDE.md §4) и блокируется
  `SELECT … FOR UPDATE`; если записанный `started_at` не NULL и не раньше
  нового — прогон устарел, транзакция откатывается; иначе строки дня
  удаляются, новый полный набор вставляется, отметка получает новый
  `started_at`, `requested_skus`, `raw_document_ids` и число строк,
  а `first_started_at` —
  `COALESCE(first_started_at, новый started_at)`. Повторная запись того же
  прогона (тот же `started_at`) ничего не меняет — идемпотентно.
  Удаляются только производные строки того же дня — raw остаётся, снимок
  восстановим;
- **битемпоральность — у отметки дня**, а не у строки: `first_started_at`
  (первый полный снимок дня) и `started_at` (последнее обновление). Строка
  при замене дня пересоздаётся, её время первой загрузки было бы ложным;
  прослеживаемость строки — её `raw_document_id`;
- **прошлые дни не пересчитываются.** Окно пересчёта и глубокий рескан
  ADR-006 к снимкам неприменимы: метод отдаёт только текущее состояние,
  даты в запросе нет. Их буквальное применение либо повторило бы один и тот
  же запрос десятки раз (ADR-028), либо записало бы сегодняшний остаток
  в прошлые даты. День D меняется только прогонами, начатыми в день D.

- Склад, а не кластер, — потому что так отдаёт площадка, и кластер из строки
  не теряется. Свести к кластеру — дело запроса; обратное невозможно.
- **ПВЗ входят в остаток кластера.** Строки с отрицательным `warehouse_id`
  (`ПВЗ_<n>`) — 9% доступного остатка; их Ozon относит к кластеру пункта
  и продаёт покупателям этого кластера.
- **`ads_cluster` хранится как `numeric(12,4)`**, из JSON-числа через
  строку с фиксированной точностью — это скорость продаж, не деньги, и
  рекомендации на ней не строятся: спрос считаем сами по продажам
  (вариант A), `ads_cluster` — рядом для сверки с кабинетом.
- Индексы: первичный ключ начинается с `(company_id, marketplace_account_id,
  snapshot_date)` — отбор по дням компании идёт по нему.

**Расписание** — раз в сутки в `rescanHour` и при подключении кабинета,
сообщение `FetchOzonStocksMessage` на транспорте ingestion. Внутридневной
ноль снимком не виден — это часть определения «дня без остатка».

**Контроль свежести** (`NotifyStaleAccountsAction`) — остатки в списке
под контролем, **по отметке полного прогона, а не по raw**: снимок полезен
только целым, а прогон, стабильно обрывающийся на последней пачке, по raw
выглядел бы живым. Отметка обновляется каждым полным прогоном.
Исключение закреплено в CLAUDE.md «Наблюдаемость».

**`/v1/cluster/list` и `/v2/analytics/stock_on_warehouses` не используются.**

### Альтернативы, которые отвергли

- **`/v2/analytics/stock_on_warehouses`** — остаток по складу без кластера,
  без ПВЗ, названия складов в другом регистре; всё, что в нём есть, есть
  в выбранном методе.
- **Сумма из `/v3/product/info/list`** — нет разбивки по складам;
  рекомендацию по кластерам из неё не построить.
- **Зерно SKU × кластер** — теряет склад, а свести обратно нельзя; экран
  «что где лежит» и сверка с кабинетом по складу стали бы невозможны.
- **Справочник кластеров из `/v1/cluster/list`** — нет трёх зарубежных
  кластеров, которые есть в продажах и остатках; кластер и так приходит
  в каждой строке остатков.
- **Брать спрос Ozon (`ads_cluster`) вместо своего** — методика Ozon
  не раскрыта, окно неизвестно; спрос по кластеру доставки мы считаем
  из своих продаж и можем объяснить клиенту (объяснимость расчёта).
- **Снимок каждый тик (15 минут)** — ×96 строк в сутки ради внутридневного
  нуля, который рекомендации по суточному спросу не нужен.
- **Хранить только последний снимок** — поправка на дефицит (исключить
  дни без остатка из знаменателя спроса) требует истории, а её не
  восстановить задним числом.
- **Upsert строк без замены дня** — строка, пропавшая во втором снимке дня
  (остаток обнулился), осталась бы от первого; поздний устаревший ответ
  затёр бы свежий.
- **Ключ той же формы, что у других фактов** (`snapshot_date` внутри
  `source_row_id`) — единообразно, но закрывает партиционирование без смены
  первичного ключа на невосстановимой истории.

### Последствия

**Получаем:** остаток по кластерам и складам, связанный со спросом по
названию кластера без справочника; основу блока «анализ остатков»;
отделение «не поставляли» от «кончился».

**Платим:**
- ~1–2 тыс. строк в сутки на кабинет (сегодня 1152 при 52 SKU) — таблица
  растёт линейно времени и каталогу; через год при 1000 SKU — порядка
  8 млн строк, партиционирование придётся делать раньше, чем у фактов
  продаж (ключ к нему готов);
- форма ключа и способ записи отличаются от фактов событий — это новый
  класс «снимочный факт», закреплённый в CLAUDE.md §2 и §6 вместе
  с принятием этого ADR, чтобы следующий снимочный источник шёл по нему,
  а не по форме фактов событий;
- перекрывающиеся прогоны одного дня не упорядочены по запросам к площадке:
  побеждает более поздний по началу, даже если его пачки ответили раньше
  последней пачки соперника — расхождение в минуты при суточном зерне;
- вторая таблица (`stock_snapshot_run`) и транзакционная замена дня вместо
  простого upsert — сложнее обработчик;
- поправка на дефицит работает только через 28 дней снимков после
  выкладки;
- семантика `transit`/`requested` подтверждена по имени, а не по данным:
  в снимке разведки оба поля нулевые.

**На что обратить внимание потом:**
- в ответе появились ключи продолжения или SKU из пачки пропадают
  при ненулевом остатке в карточке — пересмотреть загрузку пачками;
- первая поставка в пути не отразилась в `transit`/`requested` — поправить
  формулу рекомендации до того, как ею пользуются;
- `stock_snapshot_fact` превысил 10 млн строк — решение о партиционировании
  по `snapshot_date`.
