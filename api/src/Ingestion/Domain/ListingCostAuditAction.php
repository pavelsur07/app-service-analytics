<?php

declare(strict_types=1);

namespace App\Ingestion\Domain;

/**
 * Действия над себестоимостью в аудит-журнале.
 *
 * Строки, а не enum-колонка, — по той же причине, что и у соседей
 * в Identity: список событий растёт вместе с продуктом, и миграция
 * на каждое новое действие была бы ценой без выгоды.
 *
 * Живут здесь, а не в Identity: смысл действия принадлежит модулю,
 * который его совершает. Identity журнал только хранит и про
 * себестоимость ничего не знает — зависимости строго вниз.
 *
 * Два действия, а не одно, потому что за ними стоят два разных события
 * (ADR-013): новая цена прошлое не меняет, исправление — меняет. В
 * журнале это должно читаться без разбора значений.
 */
final class ListingCostAuditAction
{
    public const string Set = 'listing_cost.set';

    public const string Corrected = 'listing_cost.corrected';

    private function __construct()
    {
    }
}
